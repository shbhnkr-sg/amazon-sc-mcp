services:
  amazon-seller-mcp:
    build: .
    container_name: amazon-seller-mcp
    restart: unless-stopped
    ports:
      - "3100:8000"
    environment:
      - PYTHONUNBUFFERED=1
    env_file:
      - .env
